好，按顺序一次输出全部 5 个文件。每个都是完整内容，包含所有优化点。5 个文件全部输出完毕。以下是每个文件的核心改动说明：

---

**output-contract.md** — 修复乱码，补充"load 校验失败"作为失败判定条件之一，与 scan.py 的校验逻辑对齐。

**scan.py** — 三处主要改动：① 新增 `validate_schema()` 函数，`load` 命令会先校验 JSON 必须字段，失败时打印缺失列表并终止，不再静默跳过；② 新增 `entry_points` 表和对应的写入/查询逻辑，补上 M1 原有字段的数据库支持；③ 新增 `--type impact-chain` 联查，跨 `impact_items + functions + modules` 三张表，输出完整影响链路。

**Prompt 模板（M1-M4）** — 统一顶层字段命名：所有模式一律用 `modules`（不再用 `module_inventory`）、`data_flows`（不再用 `dataflows`）、`api_surface` 中用 `file_path + line_number`（不再用 `file_line`）。每个模板末尾增加明确的 Required Fields 列表，与 `validate_schema()` 一一对应。

**settings.local.json** — 两处改动：① `permissions.allow` 里的具体 commit message 替换为通配符 `Bash(git commit *)`，避免后续每次 commit 都要手动授权；② hooks 的 `matcher` 从 `Bash(git merge*)` 改为纯 `"Bash"`，脚本内部自行过滤命令，修复 hook 不触发的根本原因。

**SKILL.md** — 补充模式选择表、明确 `load` 校验行为、补充 `impact-chain` 查询示例、新增"空库不触发 checkpoint"的 guardrail 条目，与其他文件保持一致。