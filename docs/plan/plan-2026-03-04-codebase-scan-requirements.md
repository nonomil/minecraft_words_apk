# 需求文档：扫描整个大型代码库

> 创建时间：2026-03-04
> 状态：需求讨论中

## 需求理解

用户要求对当前项目进行完整的代码库扫描。

### 当前项目背景
- **项目名称**：Mario Minecraft 教育游戏（英语词汇学习）
- **技术栈**：HTML5/CSS3/JavaScript (ES6), Canvas API, Web Audio API
- **代码规模**：根据 CLAUDE.md 显示，之前扫描显示 116 files, 281k tokens
- **上次扫描**：2026-03-02T00:32（2天前）
- **扫描数据**：存储在 `docs\scan\2026-03-01-full-codebase-scan\scan.db`

### 核心目标（待确认）
扫描整个代码库，生成结构化的代码地图和分析报告。

## 歧义点列表

1. **扫描范围**：
   - 是否包含所有子目录？
   - 是否包含 `node_modules/`、`.git/` 等？
   - 是否只扫描 `mario-minecraft-game_V1/apk/` 目录（活跃开发区）？

2. **扫描目的**：
   - 是为了了解项目结构？
   - 是为了后续开发任务做准备？
   - 是为了更新已有的扫描数据（2天前的）？

3. **输出格式**：
   - 需要生成新的 CODEBASE_MAP.md？
   - 需要更新 CLAUDE.md 中的扫描摘要？
   - 需要生成 SQLite 数据库？

4. **扫描深度**：
   - 快速扫描（quick）：基础文件结构
   - 中等扫描（medium）：包含函数/类定义
   - 深度扫描（very thorough）：完整依赖关系和调用链

## 复杂度判断

**场景识别**：大型代码库扫描

**触发条件**：
- ✓ 用户显式要求"扫描"
- ✓ 项目包含多个模块（mario-minecraft-game_V1/apk/src/modules/ 有 01~21 个模块）
- ✓ 代码文件数量较多（116 files）

**判断结果**：属于"大型代码库"场景

**将执行的流程**：
1. 读取 `.claude/workflows/claude-workflow-largebase.md`
2. 调用 `largebase-structured-scan` skill
3. 生成结构化扫描报告

## 待确认问题

1. 您希望扫描的主要目的是什么？
   - A) 了解整体项目结构
   - B) 为后续开发任务做准备
   - C) 更新已有的扫描数据
   - D) 其他（请说明）

2. 扫描范围是否需要限定在活跃开发目录 `mario-minecraft-game_V1/apk/`？

3. 扫描深度偏好：
   - A) 快速扫描（5-10分钟，基础结构）
   - B) 中等扫描（15-30分钟，包含函数定义）
   - C) 深度扫描（30分钟+，完整依赖分析）

## 预期产出

- [ ] 更新的 CODEBASE_MAP.md
- [ ] 更新的 CLAUDE.md 扫描摘要区块
- [ ] SQLite 扫描数据库（`docs/scan/2026-03-04-*/scan.db`）
- [ ] 入口点、核心模块、关键约束的结构化列表

## 风险评估

- **低风险**：只读操作，不修改代码
- **时间成本**：根据扫描深度，5-30+ 分钟
- **资源占用**：可能生成较大的 SQLite 数据库文件
