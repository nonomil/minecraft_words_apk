# 需求文档：背包材料使用机制优化

**创建时间**: 2026-03-05
**状态**: 需求讨论中

---

## 原始需求

背包中的材料（如炸药、末影珍珠）选择后不直接使用，而是长按武器按钮才触发使用。此外，要为背包中开出的各种材料使用添加特效，例如炸药要添加敌人身上起火的特效。

---

## 需求理解（待确认）

### 核心变更点

1. **使用触发机制变更**
   - 当前行为（推测）：选择材料后立即使用
   - 目标行为：选择材料 → 装备到武器槽 → 长按武器按钮 → 触发使用

2. **特效系统增强**
   - 为各种材料使用添加视觉特效
   - 示例：炸药 → 敌人身上起火特效

---

## 歧义点列表（需澄清）

### A. 使用触发机制
1. **"长按武器按钮"的定义**
   - 长按时长阈值是多少？（建议 0.5s-1s）
   - 是否需要进度条或视觉反馈？
   - 长按期间能否移动/跳跃？

2. **材料选择流程**
   - 选择材料后是否显示"已装备"状态？
   - 是否支持快速切换不同材料？
   - 材料用完后武器槽如何处理？

3. **与现有武器系统的关系**
   - 材料是否占用武器槽？
   - 能否同时持有武器和材料？
   - 武器按钮在不同状态下的行为：
     - 未装备材料：正常攻击
     - 已装备材料：长按使用材料，短按是否仍然攻击？

### B. 特效系统
4. **特效范围**
   - 需要为哪些材料添加特效？（炸药、末影珍珠、其他？）
   - 是否需要为所有背包材料设计特效？

5. **炸药特效细节**
   - "敌人身上起火"的表现：
     - 火焰动画持续时间？
     - 是否造成持续伤害（DOT）？
     - 火焰是否会传播到其他敌人？
   - 爆炸范围和视觉效果？

6. **末影珍珠特效**
   - 传送特效（粒子、闪光、音效）？
   - 传送距离和目标选择机制？

7. **其他材料特效**
   - 是否有其他材料需要特效？（如雪球、药水等）
   - 特效优先级排序？

### C. 技术实现
8. **涉及模块**
   - 背包系统：哪个模块？（推测 `src/modules/09-items.js` 或类似）
   - 武器系统：哪个模块？
   - 特效系统：是否已有粒子系统？（推测 `src/modules/16-particles.js`）

9. **配置驱动**
   - 材料使用效果是否通过 `config/*.json` 配置？
   - 特效参数是否可配置？

---

## 复杂度初步判断

### 预估涉及文件
- 背包/物品系统模块（1-2个文件）
- 武器/输入系统模块（1-2个文件）
- 特效/粒子系统模块（1-2个文件）
- 配置文件（`config/items.json` 或类似）
- **预估总计：4-6个文件**

### 预估改动量
- 使用触发机制：~100-150行
- 特效系统：~150-200行（取决于特效种类）
- 配置文件：~50行
- **预估总计：300-400行**

### 跨模块依赖
- 背包 ↔ 武器系统（状态同步）
- 武器 ↔ 输入系统（长按检测）
- 物品使用 ↔ 特效系统（触发特效）
- **判断：跨3个模块**

### 需求明确性
- ❌ 长按时长、视觉反馈未明确
- ❌ 材料种类和特效细节未明确
- ❌ 与现有武器系统的交互逻辑未明确

---

## 复杂度判断结论

**不满足简单模式标准**（任意1条不满足即为复杂）：
- ❌ 涉及文件 > 3 个（预估 4-6 个）
- ❌ 预估 diff > 200 行（预估 300-400 行）
- ❌ 需求有歧义（多个关键点未明确）
- ❌ 跨模块（背包、武器、输入、特效）

**路由判断**：
- 非 Debug（无 bug 报告）
- 非研究调研（无选型需求）
- 非大型代码库（文件数在可控范围）
- 非并行开发（任务耦合度高，不可拆分）
- ✅ **复杂开发模式**

---

## 待确认问题（优先级排序）

### P0（必须明确）
1. 长按武器按钮的时长阈值？
2. 材料是否占用武器槽？短按武器按钮的行为？
3. 需要为哪些材料添加特效？（列出完整清单）

### P1（建议明确）
4. 炸药特效是否造成持续伤害？持续时间？
5. 末影珍珠的传送距离和机制？
6. 是否需要长按进度条或视觉反馈？

### P2（可后续调整）
7. 特效的具体视觉风格（粒子颜色、大小、动画）
8. 音效需求

---

## 技术决策（已确认）

### A. 使用触发机制
- **长按时长阈值**：800ms（0.8秒）
- **视觉反馈**：长按时武器按钮显示圆形进度条
- **长按期间行为**：可以移动，不可跳跃（防止误触）
- **材料装备逻辑**：
  - 材料占用独立的"消耗品槽"，不影响武器槽
  - 短按武器按钮：正常攻击
  - 长按武器按钮：使用已装备的材料
  - 材料用完后自动卸载

### B. 特效系统
**材料清单及特效**：
1. **炸药（TNT）**
   - 爆炸范围：3格半径
   - 敌人起火特效：持续 3 秒，每秒造成 10% 最大生命值伤害
   - 火焰粒子：橙红色，向上飘散
   - 不传播到其他敌人

2. **末影珍珠**
   - 传送距离：鼠标点击位置或前方 5 格
   - 传送特效：紫色粒子爆发 + 闪光
   - 音效：传送"嗖"声

3. **雪球**
   - 减速效果：敌人移动速度降低 50%，持续 2 秒
   - 冰霜粒子：蓝白色

4. **岩浆桶**
   - 地面留下岩浆池：3x3 格，持续 5 秒
   - 敌人踩到持续受伤（每秒 15% 生命值）
   - 橙黄色发光效果

### C. 涉及模块（确认）
- `src/modules/09-items.js` - 背包/物品系统
- `src/modules/14-input.js` - 输入系统（长按检测）
- `src/modules/07-player.js` - 玩家状态（消耗品槽）
- `src/modules/16-particles.js` - 粒子特效系统
- `src/modules/08-enemies.js` - 敌人状态（起火、减速等 debuff）
- `config/items.json` - 物品配置

---

## 下一步

创建 worktree 并进入复杂开发流程。
