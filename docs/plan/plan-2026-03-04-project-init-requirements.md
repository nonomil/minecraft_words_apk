# 项目初始化需求初稿

> 创建时间：2026-03-04
> 状态：待讨论

## 需求理解

用户提出"项目初始化"任务。根据当前项目状态分析，可能的含义包括：

### 当前项目状态
- ✅ Git 仓库已初始化（master 分支）
- ✅ 工程目录结构已存在（docs/plan/, docs/memory/, docs/archive/）
- ✅ 大量 .claude/ 配置文件已添加到 Git
- ✅ CLAUDE.md 工作流规范已配置
- 📂 项目类型：Mario × Minecraft 英语学习游戏（HTML5/Canvas/JavaScript）

### 可能的初始化需求

**选项 A：运行 project-init skill**
- 分析项目结构
- 生成项目和模块的 claude-template.md 文档
- 更新 CLAUDE.md 中的项目特定信息

**选项 B：首次提交初始化**
- 将当前所有已添加的文件提交到 Git
- 创建初始 commit（当前有大量 A 状态文件未提交）

**选项 C：开发环境初始化**
- 检查依赖安装（Node.js, Android SDK 等）
- 验证构建工具链
- 运行测试确认环境正常

**选项 D：文档初始化**
- 生成项目架构文档
- 创建开发指南
- 整理现有文档结构

## 歧义点列表

1. **"初始化"的具体范围**：是指哪一类初始化？
2. **是否需要提交当前更改**：Git 中有大量 A 状态文件，是否需要先提交？
3. **是否需要运行 project-init skill**：生成项目结构文档？
4. **是否需要验证环境**：检查开发工具链是否完整？

## 复杂度判断

**待定** - 取决于用户确认的具体需求：
- 如果仅是 Git 提交：简单模式（1 个文件操作）
- 如果是 project-init skill：简单模式（调用 skill）
- 如果是完整环境验证：可能需要复杂模式

## 待确认问题

1. 你说的"项目初始化"具体指什么？
   - [ ] 运行 project-init skill 生成项目文档
   - [ ] 提交当前所有更改到 Git
   - [ ] 验证开发环境和工具链
   - [ ] 其他（请说明）

2. 当前 Git 中有大量已添加但未提交的文件，是否需要先处理？

3. 是否需要我先读取项目结构，了解代码库后再执行初始化？

## 用户确认

✅ **选择：C. 完整初始化流程**

### 执行计划

**Step 1：提交当前更改**
- 检查 git status，确认待提交文件
- 创建初始提交：`git commit -m "init: 添加 Claude Code 工作流配置和项目文档"`

**Step 2：运行 project-init skill**
- 调用 `project-init` skill 分析项目结构
- 生成项目架构文档
- 更新 CLAUDE.md 项目特定信息

**Step 3：验证开发环境**
- 检查 Node.js 版本
- 验证项目依赖
- 确认构建工具链可用

### 复杂度判断

**判断结果：简单模式**

依据：
- ✅ 涉及文件 ≤ 3 个（主要是 Git 操作和 skill 调用）
- ✅ 预估 diff ≤ 200 行（仅提交现有文件，无代码修改）
- ✅ 需求明确，无歧义
- ✅ 单模块内操作

### 下一步

等待用户说"确认"或"开始"后执行。
