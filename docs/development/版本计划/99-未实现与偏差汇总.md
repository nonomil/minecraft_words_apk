# 未实现与偏差汇总（2026-02-16 修订）

审计范围：`apk/` 目录  
结论口径：以 `apk/src/modules` 实际代码为准

---

## 总结

- `v1.9.0` ~ `v1.11.3`：已实现（含前期缺口补齐）  
- `v1.12.0`：已实现  
- `v1.13.0`：已实现  
- `v1.14.0`：已实现（粒子池采用类型池化方案）  
- `v1.15.0`：已实现  

当前在上述计划范围内无“未实现”项。

---

## 关键纠偏说明

1. 之前“`v1.13.0/v1.14.0/v1.15.0` 部分未实现”的判断已过时。  
2. `v1.14.0` 的粒子池已落地于 `apk/src/modules/06-biome.js` 与 `apk/src/modules/13-game-loop.js`。  
3. `v1.15.0` 的节流扩展已覆盖装饰物和粒子，且 `apk/src/main.js` 已删除。  

---

## 后续建议（不属于本汇总缺口）

1. 为粒子池增加性能基准测试（帧时与内存波动对比）。  
2. 在 Playwright 中增加稳定性重试或浏览器启动保护，降低环境波动导致的偶发失败。  
