# vA 运营前修正与最小开发 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在不增加重运营负担的前提下，用半天到一天完成运营前关键修正，降低退款和售后，并准备进入30天执行节奏。  
**Architecture:** 分三段执行：先修正文案与素材基线（4项），再做vA最小开发（3项），最后做一次可回归核验并打包发布。每个任务都包含文件、步骤、命令、验收标准。  
**Tech Stack:** `Game.html` + Vanilla JS（`src/modules/*.js`）+ Playwright E2E（`tests/e2e/specs`）+ Markdown运营文档。

---

## 总体时间盒

- 阶段A（运营前修正）：45-60分钟
- 阶段B（vA最小开发）：3-5小时
- 阶段C（验证与发布）：60-90分钟
- 总计：约半天到一天

## 执行顺序（严格按序）

1. 先完成阶段A，再进入代码改动。
2. 阶段B每个任务完成后都要做一次最小验证。
3. 阶段C通过后再进入内容发布与成交SOP执行。

### Task 1: 统一运营基线文案（4项修正）

**Files:**
- Modify: `docs/plans/2026-02-27-轻量商业路线综合包/02-30天执行计划-小红书咸鱼.md`
- Modify: `docs/plans/2026-02-27-轻量商业路线综合包/03-内容发布与成交SOP.md`
- Modify: `docs/plans/2026-02-27-轻量商业路线综合包/README.md`

**Step 1: 截图数量统一为6张**

- 把所有“8张截图”统一改为“6张核心截图”。
- 6张固定清单：单词卡弹出、单词门挡路、复活测验、多群系环境、词库选择、战斗+单词同框。

**Step 2: 咸鱼标题去IP关键词**

- 在文档中明确“禁用词”：`Minecraft`、`马里奥`、`我的世界`。
- 在文档中明确“替代词”：`方块闯关`、`像素冒险`、`英语闯关学习`。

**Step 3: 设备兼容说明固定模板**

- 在商品详情模板固定加入：`建议 Android 8.0 及以上，低版本可能出现发音异常。`

**Step 4: 私信首句固定加入iOS排除**

- 在首条回复模板固定加入：`安卓手机可用，iOS 暂不支持。`

**Step 5: 自检**

Run:
```bash
rg -n "8 张|8张|Minecraft|马里奥|Android 8.0|iOS 暂不支持" docs/plans/2026-02-27-轻量商业路线综合包
```

Expected:
- “8张截图”不再出现在执行正文。
- Android和iOS说明都可被检索到。
- 违规IP词只出现在“风险说明/禁用词”语境，不出现在发布模板示例。

**Step 6: Commit**

```bash
git add docs/plans/2026-02-27-轻量商业路线综合包/02-30天执行计划-小红书咸鱼.md docs/plans/2026-02-27-轻量商业路线综合包/03-内容发布与成交SOP.md docs/plans/2026-02-27-轻量商业路线综合包/README.md
git commit -m "docs: align ops baseline (6 screenshots, no IP keywords, android/ios notes)"
```

### Task 2: 设置页增加“测试发音”按钮（UI）

**Files:**
- Modify: `Game.html`

**Step 1: 写失败用例（先测后改）**

- 新建一个E2E用例，断言设置面板存在测试发音按钮。

Create:
- `tests/e2e/specs/opt-0227-task1-tts-selftest-button.spec.mjs`

**Step 2: 跑用例确认失败**

Run:
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0227-task1-tts-selftest-button.spec.mjs
```

Expected: FAIL（按钮尚不存在）

**Step 3: 最小实现**

- 在设置面板操作区增加按钮：`id="btn-speech-test"`，文案为`测试发音`。
- 保持现有布局不破坏移动端换行。

**Step 4: 回归测试**

Run:
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0227-task1-tts-selftest-button.spec.mjs
```

Expected: PASS

**Step 5: Commit**

```bash
git add Game.html tests/e2e/specs/opt-0227-task1-tts-selftest-button.spec.mjs
git commit -m "feat: add settings tts self-test button"
```

### Task 3: 接入“测试发音”逻辑与错误提示

**Files:**
- Modify: `src/modules/16-events.js`
- Modify: `src/modules/12-challenges.js`
- Optional Modify: `src/modules/03-audio.js`

**Step 1: 写失败用例**

- 断言点击 `#btn-speech-test` 后会触发 `window.speakWord(...)`。
- 用例里可先对 `window.speakWord` 打桩计数。

Create:
- `tests/e2e/specs/opt-0227-task2-tts-selftest-action.spec.mjs`

**Step 2: 跑用例确认失败**

Run:
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0227-task2-tts-selftest-action.spec.mjs
```

Expected: FAIL（按钮虽存在但未绑定）

**Step 3: 最小实现**

- 在 `wireSettingsModal()` 中获取 `btn-speech-test` 并绑定点击事件。
- 事件逻辑建议：
  - 若 `settings.speechEnabled !== true`：toast 提示先开启朗读。
  - 若未解锁音频：先 `unlockAudio()`。
  - 调用 `speakWord({ en: "hello", zh: "你好" })` 触发测试。
  - 异常时 toast 给出“设备不支持/请重试”。

**Step 4: 回归测试**

Run:
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0227-task1-tts-selftest-button.spec.mjs tests/e2e/specs/opt-0227-task2-tts-selftest-action.spec.mjs
```

Expected: PASS

**Step 5: Commit**

```bash
git add src/modules/16-events.js src/modules/12-challenges.js src/modules/03-audio.js tests/e2e/specs/opt-0227-task2-tts-selftest-action.spec.mjs
git commit -m "feat: wire tts self-test action in settings modal"
```

### Task 4: 档案页增加“备份/恢复存档”快捷入口

**Files:**
- Modify: `Game.html`
- Modify: `src/modules/08-account.js`
- Optional Modify: `tests/e2e/specs/opt-0226-task1-save-transfer.spec.mjs`

**Step 1: 写失败用例**

- 断言档案页（`#profile-modal`）存在两个快捷按钮：
  - `#btn-profile-export-save`
  - `#btn-profile-import-save`

Create:
- `tests/e2e/specs/opt-0227-task3-profile-save-shortcuts.spec.mjs`

**Step 2: 跑用例确认失败**

Run:
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0227-task3-profile-save-shortcuts.spec.mjs
```

Expected: FAIL

**Step 3: 最小实现**

- 在 `#profile-modal .profile-actions` 下新增“备份存档码/恢复存档码”两个按钮。
- 在 `wireProfileModal()` 中绑定新按钮到 `handleExportSave()` / `handleImportSave()`。
- 保留原保存弹窗中的导出/导入按钮，避免功能回退。

**Step 4: 回归测试**

Run:
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0226-task1-save-transfer.spec.mjs tests/e2e/specs/opt-0227-task3-profile-save-shortcuts.spec.mjs
```

Expected: PASS

**Step 5: Commit**

```bash
git add Game.html src/modules/08-account.js tests/e2e/specs/opt-0227-task3-profile-save-shortcuts.spec.mjs
git commit -m "feat: add profile quick entry for save export/import"
```

### Task 5: 学习统计补充“正确率”字段

**Files:**
- Modify: `src/modules/08-account.js`
- Modify: `tests/e2e/specs/opt-0226-task5-learning-stats.spec.mjs`

**Step 1: 写失败用例**

- 在现有 `task5` 用例中新增断言：`renderLearningStats()` 返回中含“正确率”。
- 验证公式：`correct / (correct + wrong)`，保留到整数百分比。

**Step 2: 跑用例确认失败**

Run:
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0226-task5-learning-stats.spec.mjs
```

Expected: FAIL（尚未渲染正确率）

**Step 3: 最小实现**

- 在 `renderLearningStats(account)` 中聚合总 `correct` 与总 `wrong`。
- 新增第三个统计卡片：`正确率 <strong>xx%</strong>`。
- 当无答题记录时显示 `0%`。

**Step 4: 回归测试**

Run:
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0226-task5-learning-stats.spec.mjs
```

Expected: PASS

**Step 5: Commit**

```bash
git add src/modules/08-account.js tests/e2e/specs/opt-0226-task5-learning-stats.spec.mjs
git commit -m "feat: add accuracy metric to learning stats panel"
```

### Task 6: 联调核验与发布

**Files:**
- Verify: `Game.html`
- Verify: `src/modules/08-account.js`
- Verify: `src/modules/12-challenges.js`
- Verify: `src/modules/16-events.js`
- Verify: `docs/plans/2026-02-27-轻量商业路线综合包/*.md`

**Step 1: 运行关键自动化**

Run:
```bash
npx playwright test -c tests/e2e/playwright.config.mjs tests/e2e/specs/opt-0226-task1-save-transfer.spec.mjs tests/e2e/specs/opt-0226-task5-learning-stats.spec.mjs tests/e2e/specs/opt-0227-task1-tts-selftest-button.spec.mjs tests/e2e/specs/opt-0227-task2-tts-selftest-action.spec.mjs tests/e2e/specs/opt-0227-task3-profile-save-shortcuts.spec.mjs
```

Expected: 全PASS

**Step 2: 手工回归（必须）**

1. 打开设置页，点击“测试发音”，确认有发音或明确失败提示。  
2. 打开档案页，直接看到“备份/恢复存档”按钮并可触发。  
3. 打开学习统计，确认“正确率”显示正确。  
4. 私信模板首句含“iOS 暂不支持”。  
5. 咸鱼标题模板不含IP词，详情含“Android 8.0+”。  

**Step 3: 构建同步**

Run:
```bash
npm run build
```

Expected:
- `scripts/sync-web.js` 执行完成，无报错。

**Step 4: 发布前提交**

```bash
git add Game.html src/modules/08-account.js src/modules/12-challenges.js src/modules/16-events.js tests/e2e/specs docs/plans/2026-02-27-轻量商业路线综合包
git commit -m "release: vA pre-ops fixes for tts diagnostics, save shortcuts, and learning accuracy"
```

## 上线闸门（必须同时满足）

1. 自动化用例全通过。
2. 手工回归5项全通过。
3. 运营文档4项修正已落地。
4. 才进入`02-30天执行计划-小红书咸鱼.md`的Day 1执行。

## vB 预告（不在本次范围）

- 弱词一键专项复习（`09-vocab.js`）
- 连续学习天数与今日任务（`08-account.js` + `storage.js`）
- Web试玩页最小转化漏斗（`Game.html` + 引导文案）
