# 发布说明 v1.18.56

发布日期：2026-02-25

## 本次发布内容
- 触控区 7 个按钮图标回退为 `v1.18.42` 目标样式（`⬅️ ➡️ 🚪 🧰 🔁 ⤴️ 🗡️`）。
- HUD 顶部“重读/暂停”按钮图标恢复为 `🔊`。
- 移除 `text-size-adjust` 样式，降低 APK WebView 与网页直开之间的图标缩放偏差。
- GitHub Release 发布说明改为优先读取 `apk/docs/release/发布说明-v<version>.md`，避免旧模板长期复用。

## 升级提示
- 推送到 `main` 后将触发 Android 构建与自动发布。
