# 发布说明 v1.18.46

发布日期：2026-02-23

## 本次发布内容
- 开局弹窗标题去重，仅保留顶部主标题。
- 开局弹窗副标题补充一句：挑战关卡，获取奖励与新武器。
- APK 构建前自动同步网页资源（`build:apk` / `build:apk:release` 先执行 build + sync）。

## 升级提示
- 构建 APK 请使用 `npm run build:apk` 或 `npm run build:apk:release`，确保 web 资源与 APK 一致。
