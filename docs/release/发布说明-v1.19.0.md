# v1.19.0 发布说明（Quick Win）

## 修复
- 修复词库轮换偏斜：`switchToNextPackInOrder` 重置进度时同步清空 `runCounts`。
- 调整 Word Gate 参数并增加首次触发教学提示，提升核心学习机制可见性。
- 护甲减伤平衡同步：黄金/钻石/下界合金减伤重设，且有甲时最少受 `1` 点伤害。

## 优化
- 高级学习设置英文标签全部中文化，降低家长配置门槛。
- 单词卡停留时长新增 `2秒` / `3秒`，默认由 `0.9s` 调整为 `1.2s`。
- 普通宝箱中 `word_card` 掉落权重下调，实体资源掉落体感增强。

## 版本信息
- `versionName`: `1.19.0`
- `versionCode`: `59`
